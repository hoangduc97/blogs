version: "3.4"

services:
    api:
        build: ./api
        volumes:
            - ./api:/app/api
        environment:
            - DB_URI=${DB_URI}
            - PORT=${API_PORT}
        ports:
            - ${API_PORT}:${API_PORT}
        restart: unless-stopped
        networks:
            - blogs_network

    client:
        build: ./client
        stdin_open: true
        volumes:
            - ./client:/app/client
        environment:
            - PORT=${CLIENT_PORT}
            - API_PORT=${API_PORT}
        ports:
            - ${CLIENT_PORT}:${CLIENT_PORT}
        restart: unless-stopped
        networks:
            - blogs_network

    db:
        image: mongo
        container_name: mongo_blogs
        volumes:
            - .docker/data/db:/data/db
        restart: unless-stopped
        ports:
            - ${DB_PORT}:${DB_PORT}
        networks:
            - blogs_network

networks:
    blogs_network:
        driver: bridge
