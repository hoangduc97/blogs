version: "3.4"

services:
    api:
        build: ./api
        container_name: api_blogs
        volumes:
            - ./api:/app/api
        env_file: .env
        ports:
            - ${API_PORT}:${API_PORT}
        restart: unless-stopped
        networks:
            - private_network

    # client:
    #     build: ./client
    #     container_name: client_blogs
    #     volumes:
    #         - ./client:/app/client
    #     environment:
    #         - PORT=${CLIENT_PORT}
    #         - API_PORT=${API_PORT}
    #     ports:
    #         - ${CLIENT_PORT}:${CLIENT_PORT}
    #     restart: unless-stopped
    #     networks:
    #         - private_network

networks:
    private_network:
        driver: bridge
